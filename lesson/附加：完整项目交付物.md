附加：完整项目交付物——所有文件一览

> **写给需要“照镜子”的你**：前面十章，我们从零开始，一步步构建了一个 Agent 驱动的 SDD+TDD 注册系统。你现在可能会想：“能不能把最终版本的所有文件，完整地给我看一眼？我想确认我的项目结构、代码、测试是不是和课程一致。”
>
> 这一章就是你的“镜子”——我们会把整个项目的组织结构、每一个文件的完整内容、每一行代码都摊开在你面前。你可以对照、复制、运行、修改，把它作为你未来项目的模板。更重要的是，你会看到那些**比代码更重要的文件**——Spec.md、Agent.md、Task.md、traceability.md——它们才是这个项目的灵魂。

---

## 11.1 项目目录结构

```
sdd-tdd-agent-login/
├── .gitignore                  # Git 忽略规则
├── pytest.ini                  # pytest 配置与标记注册
├── requirements.txt            # 依赖清单
├── Agent.md                    # Agent 角色定义与行为约束
├── Task.md                     # 当前任务清单与进度
├── SystemPrompt.md             # 系统上下文与编码偏好
├── README.md                   # 项目说明（可选）
├── traceability.md             # 需求追溯矩阵（Agent 自动维护）
│
├── database.py                 # 数据库连接与表初始化（SQLite）
├── register.py                 # 核心业务逻辑 + 数据仓库层
├── app.py                      # Flask Web API 入口
│
├── test_register.py            # 注册功能单元测试（18 个）
├── test_api.py                 # API 端到端集成测试（5 个）
│
├── docs/
│   └── Spec.md                 # 用户注册功能规格文档（单一真相来源）
│
└── venv/                       # Python 虚拟环境（不提交到 Git）
```

---

## 11.2 `.gitignore`

```gitignore
# .gitignore
venv/
__pycache__/
*.pyc
*.pyo
app.db
report.html
.env
.DS_Store
```

---

## 11.3 `pytest.ini`

```ini
# pytest.ini
# pytest 配置文件，定义自定义标记和运行选项

[pytest]
markers =
    email_validation: 邮箱格式与长度校验（对应 Spec INVALID_EMAIL_FORMAT）
    password_validation: 密码强度与长度校验（对应 Spec PASSWORD_TOO_SHORT / TOO_LONG / WEAK）
    duplicate_check: 重复注册校验（对应 Spec EMAIL_ALREADY_REGISTERED）
    normalization: 邮箱规范化处理（对应 Spec 边界条件-大小写+空格）
    success_path: 正常注册成功路径（对应 Spec 成功响应）
    api: API 端到端集成测试（对应 Spec HTTP 状态码和响应体）
```

---

## 11.4 `requirements.txt`

```text
bcrypt==4.1.3
Flask==3.1.0
pytest==8.3.4
pytest-html==4.1.1
```

> **注意**：你的实际版本号可能略有不同，这是正常的。核心依赖是 `pytest`、`pytest-html`、`flask`、`bcrypt`。SQLite 是 Python 自带库，不需要额外安装。

---

## 11.5 `Agent.md`

```markdown
---
agent:
  name: sdd-tdd-python-dev
  version: 1.0.0
  role: full-stack-developer
  language: python
  framework: flask
  database: sqlite
  testing: pytest
---

# Agent: SDD+TDD Python 开发者

## 角色
你是一位严格遵循 SDD（规格驱动开发）和 TDD（测试驱动开发）的 Python 后端开发专家。
你的用户是一位大学生或初级程序员，你的产出将帮助他们学习专业的开发方法。

## 核心能力
- 解析 Spec.md 中的结构化需求规格
- 基于规格自动生成 pytest 测试用例（TDD-Red 阶段）
- 编写刚好通过测试的最小实现代码（TDD-Green 阶段）
- 在测试全绿后执行代码重构（TDD-Refactor 阶段）
- 集成 SQLite 数据库和 Flask Web 框架
- 自动生成需求追溯矩阵和测试报告

## 行为约束

### 你必须做的
1. ✅ **每次任务前**，先读取 Spec.md 和 Task.md
2. ✅ **在写任何实现代码之前**，先写测试代码
3. ✅ **每次代码修改后**，立即运行 `pytest` 确认全绿
4. ✅ **使用 bcrypt** 进行密码哈希
5. ✅ **使用 UUID v4** 作为用户 ID
6. ✅ **所有错误码**必须与 Spec.md 中定义的一致
7. ✅ **完成任务后**，更新 Task.md 中的进度

### 你绝不能做的
1. ❌ **绝不**在 Spec.md 未定义的情况下编写实现代码
2. ❌ **绝不**在测试全红之前编写实现代码
3. ❌ **绝不**在数据库中存储明文密码
4. ❌ **绝不**过度设计——只写刚好通过测试的代码
5. ❌ **绝不**跳过人类审查环节——生成代码后，必须等待确认才能继续

## 开发工作流
1. 读取 Spec.md → 理解需求
2. 编写测试代码 → 确认红灯
3. 编写最小实现 → 确认绿灯
4. 运行全部测试 → 确保全绿
5. 重构优化 → 保持全绿
6. 更新 Task.md 和 traceability.md

## 输出格式
- 代码块使用 ```python 标记，并标注文件名
- 错误修复使用 diff 格式展示变更
- 完成任务后输出简要总结（通过了几个测试、生成了多少行代码）
```

---

## 11.6 `Task.md`

```markdown
---
task:
  id: REG-001
  title: 实现用户邮箱密码注册功能
  status: completed
  priority: high
  assigned_to: sdd-tdd-python-dev
---

# 任务: 用户邮箱密码注册功能

## 任务描述
基于 Spec.md 中的需求规格，完整实现用户注册功能，
包括输入校验、密码哈希、数据持久化和 REST API 端点。

## 子任务

### ✅ 第一阶段: 准备工作
- [x] 阅读并理解 Spec.md 中的全部需求
- [x] 确认项目依赖已安装（flask, pytest, bcrypt）

### ✅ 第二阶段: TDD-Red（编写测试）
- [x] 编写成功注册测试
- [x] 编写邮箱格式校验测试（参数化：缺 @、空串、超长）
- [x] 编写密码强度校验测试（参数化：过短、过长、纯字母、纯数字）
- [x] 编写重复注册测试
- [x] 编写邮箱规范化测试（大小写、空格）
- [x] 补充：邮箱规范化分离测试（仅空格、仅大写）
- [x] 补充：数据库 UNIQUE 约束验证测试

### ✅ 第三阶段: TDD-Green（最小实现）
- [x] 实现邮箱格式校验逻辑
- [x] 实现密码强度校验逻辑
- [x] 实现重复注册检查逻辑
- [x] 实现邮箱规范化逻辑
- [x] 实现 bcrypt 密码哈希
- [x] 实现 UUID v4 用户 ID 生成

### ✅ 第四阶段: TDD-Refactor（重构优化）
- [x] 修复类型提示（Optional[UserRepository]）
- [x] 添加 __all__ 声明，隐藏私有 API
- [x] 优化 register 函数的 docstring（增加 Raises 和 Example）
- [x] 确认 18/18 测试全绿

### ✅ 第五阶段: 集成与交付
- [x] 创建 database.py（SQLite 连接与表初始化）
- [x] 创建 app.py（Flask API 入口 + POST /api/register 端点）
- [x] 编写 API 集成测试（5 个场景，全部通过）
- [x] 生成需求追溯矩阵（traceability.md，29 条规格全部可追溯）
- [x] 生成 HTML 测试报告（report.html，24/24 全绿）

## 验收标准
- [x] 全部 pytest 测试通过（单元 + 集成）
- [x] 数据库中密码为 bcrypt 哈希格式
- [x] API 返回格式符合 Spec.md 定义
- [x] traceability.md 覆盖所有规格条目
- [x] 代码无魔法值，结构清晰

## 项目状态
- **总测试数**: 24（18 单元 + 5 集成 + 1 数据库约束）
- **覆盖率**: 96.6%（28/29，1 条为架构约束无需测试）
- **全绿状态**: ✅ 24 passed, 0 failed
- **最后更新**: 2025-01-21
```

---

## 11.7 `SystemPrompt.md`

```markdown
# 系统提示词

## 项目上下文
- 项目名称: sdd-tdd-agent-login
- 项目目标: 教学演示 SDD+TDD+Agent 开发模式
- 用户群体: 大学生 / Python 初学者
- 语言: Python 3.10+
- Web 框架: Flask 3.x
- 数据库: SQLite 3（通过 Python 内置 sqlite3 模块操作）
- 测试框架: pytest 8.x（含 pytest-html 报告插件）
- 密码安全: bcrypt 4.x
- 包管理: pip + venv

## 项目文件结构
- `Spec.md` — 唯一的需求真相来源（Agent 必须严格遵守）
- `Agent.md` — Agent 的角色定义和行为约束
- `Task.md` — 当前开发任务的待办清单
- `SystemPrompt.md` — 本文件，提供项目上下文
- `register.py` — 核心业务逻辑
- `app.py` — Flask Web API 入口
- `database.py` — SQLite 数据库连接与表初始化
- `test_register.py` — 注册功能单元测试套件
- `test_api.py` — API 端到端集成测试套件
- `pytest.ini` — pytest 配置与自定义标记
- `traceability.md` — 需求追溯矩阵（Agent 负责自动维护）

## 编码偏好
- 使用 Python 类型提示（如 `def register(email: str, password: str) -> dict:`）
- 私有辅助函数以下划线开头（如 `_validate_email`）
- 常量使用大写加下划线（如 `EMAIL_MAX_LENGTH`）
- 错误码常量集中在文件顶部定义
- 数据库操作封装在 UserRepository 类中
- 测试函数名清晰描述场景（如 `test_register_duplicate_email`）

## 注意事项
- 代码是教学用的，注释和 docstring 要清晰易懂
- 每个测试函数都有 docstring 说明它验证的是 Spec.md 的哪一条
- 所有生成的文件使用 UTF-8 编码
```

---

## 11.8 `docs/Spec.md`

```markdown
---
spec:
  name: 用户注册功能
  version: 1.0.0
  status: approved
  owner: product-team
  created: 2025-01-15
  updated: 2025-01-20
  tags:
    - authentication
    - user-management
    - registration
---

# 用户注册功能规格

## 功能描述

提供用户注册能力。
用户通过提供符合格式的邮箱和符合安全规则的密码，创建一个新的用户账户。
注册成功后返回用户唯一标识和规范化后的邮箱信息。
注册不触发自动登录（功能分离）。

## 输入参数

### email
- **类型**: `string`
- **必填**: 是
- **约束**:
  1. 必须包含 `@` 符号
  2. 长度不得超过 255 字符
  3. 首尾空格自动去除
  4. 统一转换为小写字母
- **合法示例**: `"user@example.com"`, `"  User@Example.com  "`
- **非法示例**: `"notanemail"`, `""`, `"a"*256 + "@example.com"`
- **错误码**: `INVALID_EMAIL_FORMAT`

### password
- **类型**: `string`
- **必填**: 是
- **约束**:
  1. 长度: 8 ≤ len ≤ 64
  2. 至少包含 1 个英文字母
  3. 至少包含 1 个数字
  4. 首尾空格不做自动去除
- **合法示例**: `"SecurePass123"`, `"Abcdefg1"`（刚好 8 位）
- **非法示例**: `"Abc1234"`（7 位）, `"abcdefgh"`（无数字）, `"12345678"`（无字母）
- **错误码**:
  - `PASSWORD_TOO_SHORT`（长度 < 8）
  - `PASSWORD_TOO_LONG`（长度 > 64）
  - `WEAK_PASSWORD`（不含字母或数字）

## 输出结果

### 成功响应
- **HTTP 状态码**: `201 Created`
- **响应体**:
```json
{
  "userId": "UUID v4 字符串",
  "email": "规范化后的邮箱（小写、无首尾空格）"
}
```

### 失败响应
- **格式**:
```json
{
  "error": "ERROR_CODE",
  "message": "人类可读描述"
}
```

| 错误码 | HTTP 状态码 | 触发条件 |
|--------|-------------|----------|
| `INVALID_EMAIL_FORMAT` | 400 | 邮箱不含 @ 或长度 > 255 |
| `PASSWORD_TOO_SHORT` | 400 | 密码长度 < 8 |
| `PASSWORD_TOO_LONG` | 400 | 密码长度 > 64 |
| `WEAK_PASSWORD` | 400 | 密码不含字母或数字 |
| `EMAIL_ALREADY_REGISTERED` | 409 | 邮箱已存在（不区分大小写） |

## 边界条件

| # | 场景 | 输入 | 预期结果 |
|---|------|------|---------|
| B1 | 邮箱刚好 255 字符 | 255 字符的合法邮箱 | 成功 |
| B2 | 邮箱 256 字符 | 256 字符的邮箱 | `INVALID_EMAIL_FORMAT` |
| B3 | 密码刚好 8 位 | `"Abcdefg1"` | 成功 |
| B4 | 密码刚好 64 位 | `"A"*63 + "1"` | 成功 |
| B5 | 密码含空格 | `"Valid 1 Password"` | 成功 |
| B6 | 邮箱含首尾空格和大写 | `"  User@Example.com  "` | 规范化为小写 |
| B7 | 重复注册 | 两次相同邮箱 | 第二次 409 |
| B8 | 大小写不同视为同一邮箱 | `"A@B.COM"` 和 `"a@b.com"` | 视为重复 |

## 业务规则

1. 密码使用 **bcrypt** 哈希存储，禁止明文
2. 用户 ID 使用 **UUID v4** 生成
3. 注册成功不自动登录
4. 邮箱唯一性不区分大小写
5. 依赖数据库 UNIQUE 约束处理并发
```

---

## 11.9 `database.py`

```python
# database.py
"""数据库连接与初始化模块。

使用 SQLite 作为轻量级持久化存储。
"""

import sqlite3
import os

DB_PATH = os.path.join(os.path.dirname(__file__), "app.db")


def get_connection():
    """获取数据库连接。
    
    配置 row_factory 使得查询结果可以通过列名访问。
    
    Returns:
        sqlite3.Connection: 已配置的数据库连接对象。
    """
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    return conn


def init_db():
    """初始化数据库表结构（如果不存在则创建）。
    
    创建 users 表，包含：
    - id: 用户唯一标识（UUID v4），主键
    - email: 邮箱地址，唯一约束，不可为空
    - password_hash: bcrypt 哈希后的密码，不可为空
    - created_at: 注册时间，自动填充当前时间戳
    """
    conn = get_connection()
    cursor = conn.cursor()
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS users (
            id TEXT PRIMARY KEY,
            email TEXT UNIQUE NOT NULL,
            password_hash TEXT NOT NULL,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
    """)
    conn.commit()
    conn.close()
```

---

## 11.10 `register.py`

```python
# register.py
"""用户注册核心业务逻辑模块。

公开 API:
    - register: 用户注册核心函数
    - UserRepository: 用户数据仓库
"""

import re
import uuid
from typing import Optional
import bcrypt
from database import get_connection

__all__ = ["register", "UserRepository"]


# ============================================================
# 常量定义
# ============================================================

EMAIL_MAX_LENGTH = 255
PASSWORD_MIN_LENGTH = 8
PASSWORD_MAX_LENGTH = 64

ERR_INVALID_EMAIL_FORMAT = "INVALID_EMAIL_FORMAT"
ERR_PASSWORD_TOO_SHORT = "PASSWORD_TOO_SHORT"
ERR_PASSWORD_TOO_LONG = "PASSWORD_TOO_LONG"
ERR_WEAK_PASSWORD = "WEAK_PASSWORD"
ERR_EMAIL_ALREADY_REGISTERED = "EMAIL_ALREADY_REGISTERED"


# ============================================================
# 数据仓库层
# ============================================================

class UserRepository:
    """用户数据存储，基于 SQLite 数据库。"""
    
    def exists(self, email: str) -> bool:
        """检查邮箱是否已注册。"""
        conn = get_connection()
        cursor = conn.cursor()
        cursor.execute(
            "SELECT COUNT(*) FROM users WHERE email = ?",
            (email,)
        )
        count = cursor.fetchone()[0]
        conn.close()
        return count > 0
    
    def save(self, email: str, password_hash: str, user_id: str) -> None:
        """保存新用户到数据库。"""
        conn = get_connection()
        cursor = conn.cursor()
        cursor.execute(
            "INSERT INTO users (id, email, password_hash) VALUES (?, ?, ?)",
            (user_id, email, password_hash)
        )
        conn.commit()
        conn.close()


# ============================================================
# 私有辅助函数
# ============================================================

def _validate_email(email: str) -> str:
    """校验邮箱格式，返回规范化后的邮箱。"""
    normalized = email.strip().lower()
    if "@" not in normalized or len(normalized) > EMAIL_MAX_LENGTH:
        raise ValueError(ERR_INVALID_EMAIL_FORMAT)
    return normalized


def _validate_password(password: str) -> None:
    """校验密码规则，不满足则抛出 ValueError。"""
    if len(password) < PASSWORD_MIN_LENGTH:
        raise ValueError(ERR_PASSWORD_TOO_SHORT)
    if len(password) > PASSWORD_MAX_LENGTH:
        raise ValueError(ERR_PASSWORD_TOO_LONG)
    if not re.search(r"[a-zA-Z]", password):
        raise ValueError(ERR_WEAK_PASSWORD)
    if not re.search(r"[0-9]", password):
        raise ValueError(ERR_WEAK_PASSWORD)


def _hash_password(password: str) -> str:
    """对密码进行 bcrypt 哈希处理。"""
    salt = bcrypt.gensalt()
    hashed = bcrypt.hashpw(password.encode('utf-8'), salt)
    return hashed.decode('utf-8')


# ============================================================
# 公开 API
# ============================================================

def register(email: str, password: str, repo: Optional[UserRepository] = None) -> dict:
    """用户注册。
    
    完整流程（按 Spec.md 定义）：
    1. 校验并规范化邮箱（去空格、转小写、检查格式和长度）
    2. 校验密码强度（长度 8-64、至少一个字母和一个数字）
    3. 检查邮箱是否已被注册（不区分大小写）
    4. 生成 UUID v4 作为用户 ID
    5. 使用 bcrypt 对密码做哈希
    6. 保存用户到数据库
    7. 返回 userId 和 email
    
    Args:
        email: 用户提供的邮箱地址，可包含首尾空格和大写字母。
        password: 用户提供的密码，长度 8-64，需同时包含字母和数字。
        repo: UserRepository 实例，用于数据持久化。
            如果为 None，将创建默认实例。
        
    Returns:
        dict: 包含两个键的字典：
            - userId (str): UUID v4 格式的用户唯一标识
            - email (str): 规范化后的邮箱地址（小写、无首尾空格）
        
    Raises:
        ValueError: 当输入不满足规则时抛出，错误信息为对应的错误码：
            - INVALID_EMAIL_FORMAT: 邮箱格式非法或长度超过 255
            - PASSWORD_TOO_SHORT: 密码长度不足 8
            - PASSWORD_TOO_LONG: 密码长度超过 64
            - WEAK_PASSWORD: 密码不满足字母+数字的组合要求
            - EMAIL_ALREADY_REGISTERED: 邮箱已被注册（不区分大小写）
    
    Example:
        >>> repo = UserRepository()
        >>> result = register("User@Example.com", "SecurePass123", repo)
        >>> print(result)
        {"userId": "a1b2c3d4-...", "email": "user@example.com"}
    """
    if repo is None:
        repo = UserRepository()
    
    normalized_email = _validate_email(email)
    _validate_password(password)
    
    if repo.exists(normalized_email):
        raise ValueError(ERR_EMAIL_ALREADY_REGISTERED)
    
    user_id = str(uuid.uuid4())
    password_hash = _hash_password(password)
    
    repo.save(normalized_email, password_hash, user_id)
    
    return {
        "userId": user_id,
        "email": normalized_email
    }
```

---

## 11.11 `app.py`

```python
# app.py
"""Flask Web API 应用入口。

负责：
1. 创建 Flask 应用实例
2. 定义 POST /api/register 端点
3. 解析 JSON 请求体
4. 调用 register 核心业务逻辑
5. 将 ValueError 映射为 HTTP 4xx 响应
6. 启动时自动初始化数据库
"""

from flask import Flask, request, jsonify
from register import register, UserRepository
from database import init_db

app = Flask(__name__)


# 错误码到 HTTP 状态码和人类可读消息的映射
ERROR_RESPONSE_MAP = {
    "INVALID_EMAIL_FORMAT": (400, "邮箱格式非法或长度超过 255 字符"),
    "PASSWORD_TOO_SHORT": (400, "密码长度不足 8 位"),
    "PASSWORD_TOO_LONG": (400, "密码长度超过 64 位"),
    "WEAK_PASSWORD": (400, "密码必须同时包含字母和数字"),
    "EMAIL_ALREADY_REGISTERED": (409, "该邮箱已被注册"),
}


@app.route("/api/register", methods=["POST"])
def register_api():
    """用户注册 API 端点。
    
    请求格式：
        POST /api/register
        Content-Type: application/json
        Body: {"email": "user@example.com", "password": "SecurePass123"}
    
    成功响应 (201):
        {"userId": "uuid", "email": "user@example.com"}
    
    失败响应 (4xx):
        {"error": "ERROR_CODE", "message": "人类可读描述"}
    """
    data = request.get_json()
    if not data:
        return jsonify({
            "error": "INVALID_REQUEST",
            "message": "请求体必须为 JSON 格式"
        }), 400
    
    email = data.get("email", "")
    password = data.get("password", "")
    
    try:
        repo = UserRepository()
        result = register(email, password, repo)
        return jsonify(result), 201
        
    except ValueError as e:
        error_code = str(e)
        status_code, message = ERROR_RESPONSE_MAP.get(
            error_code,
            (500, "服务器内部错误")
        )
        return jsonify({
            "error": error_code,
            "message": message
        }), status_code


if __name__ == "__main__":
    init_db()
    print("✅ 数据库表已就绪")
    print("🚀 服务器启动中... 访问 http://127.0.0.1:5000")
    app.run(debug=True)
```

---

## 11.12 `test_register.py`

```python
# test_register.py
"""用户注册功能测试套件。

本文件由 Agent 根据 docs/Spec.md 自动生成，
并经人类审查和补全。
每个测试函数的 docstring 标注了对应的规格条目。
"""

import os
import pytest
import sqlite3
from register import register, UserRepository
from database import init_db, get_connection, DB_PATH


# ============================================================
# 测试夹具
# ============================================================

@pytest.fixture(autouse=True)
def setup_database():
    """每个测试前后自动重建数据库，保证测试独立性。"""
    if os.path.exists(DB_PATH):
        os.remove(DB_PATH)
    init_db()
    yield
    if os.path.exists(DB_PATH):
        os.remove(DB_PATH)


# ============================================================
# 一、成功路径
# ============================================================

@pytest.mark.success_path
@pytest.mark.normalization
def test_register_success():
    """T1 - 对应 Spec.md「成功响应」
    
    验证：
    1. 合法邮箱 + 合法密码 → 返回字典
    2. 字典包含 userId 和 email
    3. email 为小写形式
    4. 数据库中密码为 bcrypt 哈希（以 $2b$ 开头）
    """
    repo = UserRepository()
    result = register("student@university.edu", "SecurePass123", repo)
    
    assert isinstance(result, dict)
    assert "userId" in result
    assert "email" in result
    assert result["email"] == "student@university.edu"
    
    conn = get_connection()
    cursor = conn.cursor()
    cursor.execute(
        "SELECT password_hash FROM users WHERE email = ?",
        ("student@university.edu",)
    )
    row = cursor.fetchone()
    conn.close()
    
    assert row is not None
    assert row["password_hash"] != "SecurePass123"
    assert row["password_hash"].startswith("$2b$")


# ============================================================
# 二、邮箱校验（参数化）
# ============================================================

@pytest.mark.email_validation
@pytest.mark.parametrize("email, password, expected_error, description", [
    ("notanemail", "ValidPass1", "INVALID_EMAIL_FORMAT", "缺少 @ 符号"),
    ("", "ValidPass1", "INVALID_EMAIL_FORMAT", "空字符串"),
    ("a" * 256 + "@example.com", "ValidPass1", "INVALID_EMAIL_FORMAT", "超过 255 字符"),
])
def test_register_invalid_email(email, password, expected_error, description):
    """T2 - 对应 Spec.md「INVALID_EMAIL_FORMAT」和边界条件 B2"""
    repo = UserRepository()
    with pytest.raises(ValueError, match=expected_error):
        register(email, password, repo)


# ============================================================
# 三、密码校验（参数化）
# ============================================================

@pytest.mark.password_validation
@pytest.mark.parametrize("email, password, expected_error, description", [
    ("short@test.com", "Abc1234", "PASSWORD_TOO_SHORT", "7 位密码 < 8"),
    ("long@test.com", "A" * 65 + "1", "PASSWORD_TOO_LONG", "65 位密码 > 64"),
    ("nodigit@test.com", "abcdefgh", "WEAK_PASSWORD", "纯字母无数字"),
    ("noletter@test.com", "12345678", "WEAK_PASSWORD", "纯数字无字母"),
])
def test_register_invalid_password(email, password, expected_error, description):
    """T3 - 对应 Spec.md「PASSWORD_TOO_SHORT / TOO_LONG / WEAK_PASSWORD」"""
    repo = UserRepository()
    with pytest.raises(ValueError, match=expected_error):
        register(email, password, repo)


# ============================================================
# 四、密码边界值（参数化）
# ============================================================

@pytest.mark.password_validation
@pytest.mark.parametrize("email, password, description", [
    ("border8@test.com", "Abcdefg1", "密码刚好 8 位"),
    ("border64@test.com", "A" * 63 + "1", "密码刚好 64 位"),
    ("space@test.com", "Valid 1 Password", "密码包含空格"),
])
def test_register_valid_password_boundaries(email, password, description):
    """T4 - 对应 Spec.md 边界条件 B3/B4/B5"""
    repo = UserRepository()
    result = register(email, password, repo)
    assert "userId" in result, f"边界测试失败: {description}"


# ============================================================
# 五、邮箱边界值
# ============================================================

@pytest.mark.email_validation
def test_register_email_exactly_255_chars():
    """T5 - 对应 Spec.md 边界条件 B1"""
    local_part = "a" * (255 - len("@example.com"))
    email = local_part + "@example.com"
    repo = UserRepository()
    
    result = register(email, "ValidPass1", repo)
    assert result["email"] == email.lower()


# ============================================================
# 六、重复注册
# ============================================================

@pytest.mark.duplicate_check
def test_register_duplicate_email():
    """T6 - 对应 Spec.md「EMAIL_ALREADY_REGISTERED」和边界条件 B7"""
    repo = UserRepository()
    email = "duplicate@example.com"
    
    register(email, "SecurePass123", repo)
    
    with pytest.raises(ValueError, match="EMAIL_ALREADY_REGISTERED"):
        register(email, "AnotherPass1", repo)


@pytest.mark.duplicate_check
def test_register_duplicate_email_case_insensitive():
    """T6 补充 - 对应 Spec.md 边界条件 B8"""
    repo = UserRepository()
    
    register("User@Example.com", "SecurePass123", repo)
    
    with pytest.raises(ValueError, match="EMAIL_ALREADY_REGISTERED"):
        register("user@example.com", "AnotherPass1", repo)


# ============================================================
# 七、邮箱规范化
# ============================================================

@pytest.mark.normalization
def test_register_email_normalization():
    """T7 - 对应 Spec.md 边界条件 B6"""
    repo = UserRepository()
    result = register("  User@Example.com  ", "SecurePass123", repo)
    assert result["email"] == "user@example.com"


@pytest.mark.normalization
def test_register_email_already_lowercase():
    """T7 补充 - 验证已小写邮箱不会被错误修改"""
    repo = UserRepository()
    email = "already_lowercase@example.com"
    result = register(email, "ValidPass1", repo)
    assert result["email"] == email


@pytest.mark.normalization
def test_register_email_normalization_spaces_only():
    """补充测试 - Spec.md email 约束第 3 条（仅空格）"""
    repo = UserRepository()
    result = register("  user@example.com  ", "SecurePass123", repo)
    assert result["email"] == "user@example.com"


@pytest.mark.normalization
def test_register_email_normalization_uppercase_only():
    """补充测试 - Spec.md email 约束第 4 条（仅大写）"""
    repo = UserRepository()
    result = register("USER@EXAMPLE.COM", "SecurePass123", repo)
    assert result["email"] == "user@example.com"


# ============================================================
# 八、数据库约束验证
# ============================================================

@pytest.mark.duplicate_check
def test_register_database_unique_constraint():
    """补充测试 - Spec.md 业务规则第 5 条
    
    验证数据库层面确实存在 UNIQUE 约束。
    """
    repo = UserRepository()
    register("unique_test@example.com", "SecurePass123", repo)
    
    conn = get_connection()
    cursor = conn.cursor()
    
    with pytest.raises(sqlite3.IntegrityError, match="UNIQUE constraint failed"):
        cursor.execute(
            "INSERT INTO users (id, email, password_hash) VALUES (?, ?, ?)",
            ("fake-uuid", "unique_test@example.com", "fake_hash")
        )
        conn.commit()
    
    conn.close()
```

---

## 11.13 `test_api.py`

```python
# test_api.py
"""API 层集成测试套件。

使用 Flask 测试客户端模拟 HTTP 请求，验证完整的请求-响应链路。
"""

import os
import pytest
from app import app
from database import init_db, DB_PATH


@pytest.fixture(autouse=True)
def setup_database():
    """每个 API 测试前后重建数据库。"""
    if os.path.exists(DB_PATH):
        os.remove(DB_PATH)
    init_db()
    yield
    if os.path.exists(DB_PATH):
        os.remove(DB_PATH)


@pytest.fixture
def client():
    """创建 Flask 测试客户端。"""
    app.config["TESTING"] = True
    with app.test_client() as client:
        yield client


@pytest.mark.api
def test_register_api_success(client):
    """端到端 - Spec.md「成功响应」HTTP 201"""
    response = client.post("/api/register", json={
        "email": "api_test@example.com",
        "password": "ValidPass1"
    })
    assert response.status_code == 201
    data = response.get_json()
    assert "userId" in data
    assert data["email"] == "api_test@example.com"


@pytest.mark.api
def test_register_api_duplicate(client):
    """端到端 - Spec.md「EMAIL_ALREADY_REGISTERED」HTTP 409"""
    client.post("/api/register", json={
        "email": "duplicate@example.com",
        "password": "ValidPass1"
    })
    response = client.post("/api/register", json={
        "email": "duplicate@example.com",
        "password": "ValidPass1"
    })
    assert response.status_code == 409
    data = response.get_json()
    assert data["error"] == "EMAIL_ALREADY_REGISTERED"


@pytest.mark.api
def test_register_api_weak_password(client):
    """端到端 - Spec.md「WEAK_PASSWORD」HTTP 400"""
    response = client.post("/api/register", json={
        "email": "weak@example.com",
        "password": "12345678"
    })
    assert response.status_code == 400
    data = response.get_json()
    assert data["error"] == "WEAK_PASSWORD"


@pytest.mark.api
def test_register_api_invalid_email(client):
    """端到端 - Spec.md「INVALID_EMAIL_FORMAT」HTTP 400"""
    response = client.post("/api/register", json={
        "email": "not-an-email",
        "password": "ValidPass1"
    })
    assert response.status_code == 400
    data = response.get_json()
    assert data["error"] == "INVALID_EMAIL_FORMAT"


@pytest.mark.api
def test_register_api_missing_json(client):
    """端到端 - 非法请求体返回 400"""
    response = client.post("/api/register", data="not json")
    assert response.status_code == 400
    data = response.get_json()
    assert data["error"] == "INVALID_REQUEST"
```

---

## 11.14 `traceability.md`

> 此文件由 Agent 自动生成和维护。下面给出的是最终状态的参考版本。

```markdown
# 需求追溯矩阵

> 本文档由 Agent 自动生成并维护。
> 建立 Spec.md 规格条目与测试用例的双向映射。
> 最后更新: 2025-01-21
> 测试结果: 24 passed, 0 failed

---

## 一、输入参数校验

### 1.1 邮箱格式校验

| # | 规格条目 | 对应标记 | 测试函数 | 类型 | 状态 |
|---|---------|---------|---------|------|------|
| 1 | 邮箱必须包含 @ 符号 | `email_validation` | `test_register_invalid_email[缺少 @ 符号]` | 单元 | ✅ |
| 2 | 邮箱不能为空字符串 | `email_validation` | `test_register_invalid_email[空字符串]` | 单元 | ✅ |
| 3 | 邮箱长度 ≤ 255 字符 | `email_validation` | `test_register_invalid_email[超过 255 字符]` | 单元 | ✅ |
| 4 | 邮箱刚好 255 字符允许注册 | `email_validation` | `test_register_email_exactly_255_chars` | 单元 | ✅ |
| 5 | 邮箱首尾空格自动去除 | `normalization` | `test_register_email_normalization` | 单元 | ✅ |
| 6 | 邮箱首尾空格自动去除（仅空格） | `normalization` | `test_register_email_normalization_spaces_only` | 单元 | ✅ |
| 7 | 邮箱统一转小写 | `normalization` | `test_register_email_normalization` | 单元 | ✅ |
| 8 | 邮箱统一转小写（仅大写） | `normalization` | `test_register_email_normalization_uppercase_only` | 单元 | ✅ |
| 9 | 已小写邮箱保持不变 | `normalization` | `test_register_email_already_lowercase` | 单元 | ✅ |
| 10 | API 层邮箱格式错误返回 400 | `api` | `test_register_api_invalid_email` | 集成 | ✅ |

### 1.2 密码强度校验

| # | 规格条目 | 对应标记 | 测试函数 | 类型 | 状态 |
|---|---------|---------|---------|------|------|
| 11 | 密码长度 ≥ 8 | `password_validation` | `test_register_invalid_password[7 位密码 < 8]` | 单元 | ✅ |
| 12 | 密码长度 ≤ 64 | `password_validation` | `test_register_invalid_password[65 位密码 > 64]` | 单元 | ✅ |
| 13 | 密码必须包含字母 | `password_validation` | `test_register_invalid_password[纯数字无字母]` | 单元 | ✅ |
| 14 | 密码必须包含数字 | `password_validation` | `test_register_invalid_password[纯字母无数字]` | 单元 | ✅ |
| 15 | 密码刚好 8 位允许 | `password_validation` | `test_register_valid_password_boundaries[8位]` | 单元 | ✅ |
| 16 | 密码刚好 64 位允许 | `password_validation` | `test_register_valid_password_boundaries[64位]` | 单元 | ✅ |
| 17 | 密码含空格允许 | `password_validation` | `test_register_valid_password_boundaries[空格]` | 单元 | ✅ |
| 18 | API 层弱密码返回 400 | `api` | `test_register_api_weak_password` | 集成 | ✅ |

---

## 二、输出结果

| # | 规格条目 | 对应标记 | 测试函数 | 类型 | 状态 |
|---|---------|---------|---------|------|------|
| 19 | 注册成功返回 201 + userId + email | `success_path` | `test_register_success` | 单元 | ✅ |
| 20 | API 层注册成功返回 201 | `api` | `test_register_api_success` | 集成 | ✅ |
| 21 | 重复注册返回 409 + EMAIL_ALREADY_REGISTERED | `duplicate_check` | `test_register_duplicate_email` | 单元 | ✅ |
| 22 | 大小写不同视为重复 | `duplicate_check` | `test_register_duplicate_email_case_insensitive` | 单元 | ✅ |
| 23 | API 层重复注册返回 409 | `api` | `test_register_api_duplicate` | 集成 | ✅ |
| 24 | 非法 JSON 请求返回 400 | `api` | `test_register_api_missing_json` | 集成 | ✅ |

---

## 三、业务规则

| # | 规格条目 | 对应标记 | 测试函数 | 类型 | 状态 |
|---|---------|---------|---------|------|------|
| 25 | 密码使用 bcrypt 哈希存储 | `success_path` | `test_register_success`（密码哈希断言） | 单元 | ✅ |
| 26 | 用户 ID 使用 UUID v4 | `success_path` | `test_register_success`（userId 字段存在） | 单元 | ✅ |
| 27 | 注册不触发自动登录 | — | 无测试（功能分离，不在此模块范围） | — | ⬜ |
| 28 | 邮箱唯一性不区分大小写 | `duplicate_check` | `test_register_duplicate_email_case_insensitive` | 单元 | ✅ |
| 29 | 数据库 UNIQUE 约束 | `duplicate_check` | `test_register_database_unique_constraint` | 单元 | ✅ |

---

## 覆盖率统计

| 类别 | 条目数 | 已测试 | 未测试 | 覆盖率 |
|------|--------|--------|--------|--------|
| 输入校验 | 18 | 18 | 0 | 100% |
| 输出结果 | 6 | 6 | 0 | 100% |
| 业务规则 | 5 | 4 | 1* | 80% |
| **总计** | **29** | **28** | **1** | **96.6%** |

> *注：第 27 条“注册不触发自动登录”属于功能分离的架构约束，不需要独立测试。

## 更新记录

| 日期 | 测试结果 | 变更说明 |
|------|---------|---------|
| 2025-01-21 | 24/24 ✅ | 初始版本，完成全部注册功能开发与测试 |
```

---

## 11.15 如何运行这个项目

### 第一步：创建虚拟环境并安装依赖

```bash
cd sdd-tdd-agent-login
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 第二步：运行全部测试

```bash
pytest test_register.py test_api.py -v
```

预期输出：**24 passed in X.XXs**

### 第三步：启动 Flask 服务器

```bash
python app.py
```

### 第四步：测试 API

```bash
curl -X POST http://127.0.0.1:5000/api/register \
  -H "Content-Type: application/json" \
  -d '{"email": "hello@world.com", "password": "MyPass123"}'
```

### 第五步：生成 HTML 测试报告

```bash
pytest test_register.py test_api.py -v --html=report.html --self-contained-html
```

---

## 11.16 文件关系全景图

```
┌──────────────────────────────────────────────────────────────┐
│                    你（人类指挥官）                            │
│                                                              │
│  1. 写 Spec.md（需求合同）                                    │
│  2. 写 Agent.md（行为准则）                                   │
│  3. 写 Task.md（任务清单）                                    │
│  4. 审查 Agent 的每一份产出                                   │
│  5. 最终决策：合并 or 重构 or 新需求                          │
└────────────┬─────────────────────────────────────────────────┘
             │ 指令
             ▼
┌──────────────────────────────────────────────────────────────┐
│                    Agent（AI 执行者）                          │
│                                                              │
│  1. 读取 Spec.md → 理解需求                                  │
│  2. 生成 test_register.py → 红灯                             │
│  3. 生成 register.py → 绿灯                                  │
│  4. 补全边界测试 → 安全网加密                                 │
│  5. 执行重构 → 代码更优雅                                     │
│  6. 生成 database.py + app.py → 集成                         │
│  7. 生成 test_api.py → 端到端验证                             │
│  8. 生成 traceability.md → 需求可追溯                         │
│  9. 生成 report.html → 验收报告                               │
└────────────┬─────────────────────────────────────────────────┘
             │ 交付物
             ▼
┌──────────────────────────────────────────────────────────────┐
│                    项目交付物                                 │
│                                                              │
│  📋 Spec.md          ← 单一真相来源                          │
│  🧪 test_register.py ← 18 个单元测试（可执行规格）            │
│  🧪 test_api.py      ← 5 个集成测试                          │
│  💻 register.py      ← 核心业务逻辑                          │
│  💻 app.py           ← Flask API 入口                        │
│  💻 database.py      ← SQLite 持久化层                       │
│  📊 traceability.md  ← 需求追溯矩阵                          │
│  📄 report.html      ← HTML 测试报告                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 本章小结

这一章，我们把整个项目的**全部文件**——从配置文件到规格文档，从测试代码到业务逻辑，从 Agent 治理文件到需求追溯矩阵——完整地摊开在你面前。

你可以把这一章当作：
- **对照检查清单**：你的本地项目是否和这里一致？
- **项目模板**：下一个新功能，直接复制这个结构，从 `Spec.md` 开始。
- **复习地图**：看到不熟悉的代码，回到对应章节去理解它是怎么演变过来的。

---

> **全教程终**
>
> **Spec 先写清楚，Agent 自己跑红；**
> **代码刚刚好，审查重构不能少。**
>
> **人类定方向，Agent 来执行；**
> **从登录开始，到万物皆然。**

---
